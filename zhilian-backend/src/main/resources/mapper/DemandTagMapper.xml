<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhilian.zhilianbackend.mapper.DemandTagMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zhilian.zhilianbackend.entity.DemandTag">
        <id column="id" property="id" />
        <result column="demand_id" property="demandId" />
        <result column="tag_id" property="tagId" />
        <result column="deleted" property="deleted" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, demand_id, tag_id, deleted, create_time, update_time
    </sql>

    <!-- 批量插入需求标签 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO demand_tag (demand_id, tag_id, deleted, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.demandId}, #{item.tagId}, #{item.deleted}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

</mapper>