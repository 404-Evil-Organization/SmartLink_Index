<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhilian.zhilianbackend.mapper.ManufactureTagMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zhilian.zhilianbackend.entity.ManufactureTag">
        <id column="id" property="id" />
        <result column="manufacture_id" property="manufactureId" />
        <result column="tag_id" property="tagId" />
        <result column="deleted" property="deleted" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, manufacture_id, tag_id, deleted, create_time, update_time
    </sql>

    <insert id="insertOrUpdateBatch">
        INSERT INTO manufacture_tag (manufacture_id, tag_id, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.manufactureId}, #{item.tagId}, NOW(), NOW())
        </foreach>
        ON DUPLICATE KEY UPDATE update_time = NOW()
    </insert>

</mapper>