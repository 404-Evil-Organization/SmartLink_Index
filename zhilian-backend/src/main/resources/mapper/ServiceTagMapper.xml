<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhilian.zhilianbackend.mapper.ServiceTagMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zhilian.zhilianbackend.entity.ServiceTag">
        <id column="id" property="id" />
        <result column="service_id" property="serviceId" />
        <result column="tag_id" property="tagId" />
        <result column="deleted" property="deleted" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, service_id, tag_id, deleted, create_time, update_time
    </sql>

    <!-- 批量插入或更新（存在则更新update_time，不存在则插入） -->
    <insert id="insertOrUpdateBatch" parameterType="list">
        INSERT INTO service_tag (
        service_id,
        tag_id,
        deleted,
        create_time,
        update_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.serviceId},
            #{item.tagId},
            #{item.deleted},
            NOW(),
            NOW()
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        update_time = NOW()
    </insert>

</mapper>