spring:
  datasource:
    url: jdbc:mysql://你的云数据库地址:3306/smartlink_index?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: admin
    password: 你的密码
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      minimum-idle: 5
      maximum-pool-size: 20

app:
  cors:
    allowed-origins:
      - http://你的服务器IP
    allow-credentials: true