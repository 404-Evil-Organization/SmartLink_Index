# 智链指数平台 - 作品安装说明

## 一、环境要求

### 1.1 系统环境
- **操作系统**：Windows 10/11、Linux、macOS
- **JDK版本**：Java 17 或更高版本
- **Node.js版本**：16.0.0 或更高版本
- **MySQL版本**：8.0 或更高版本
- **Maven版本**：3.6.0 或更高版本

### 1.2 推荐配置
- **内存**：8GB RAM 或更高
- **硬盘**：10GB 可用空间
- **CPU**：双核处理器或更高

## 二、环境准备

### 2.1 Java环境安装

1. **下载JDK 17**
   - 访问 [Oracle官网](https://www.oracle.com/java/technologies/downloads/#java17) 或 [OpenJDK](https://jdk.java.net/17/)
   - 下载适合您操作系统的JDK 17安装包

2. **安装JDK**
   ```bash
   # Windows
   # 运行安装程序，记住安装路径，如：C:\Program Files\Java\jdk-17
   
   # Linux/macOS
   tar -xzf openjdk-17_linux-x64_bin.tar.gz
   sudo mv jdk-17 /usr/local/
   ```

3. **配置环境变量**
   ```bash
   # Windows (在系统环境变量中设置)
   JAVA_HOME=C:\Program Files\Java\jdk-17
   PATH=%JAVA_HOME%\bin;%PATH%
   
   # Linux/macOS (在~/.bashrc或~/.zshrc中添加)
   export JAVA_HOME=/usr/local/jdk-17
   export PATH=$JAVA_HOME/bin:$PATH
   ```

4. **验证安装**
   ```bash
   java -version
   javac -version
   ```

### 2.2 MySQL数据库安装

1. **下载MySQL 8.0**
   - 访问 [MySQL官网](https://dev.mysql.com/downloads/mysql/8.0.html)
   - 下载适合您操作系统的安装包

2. **安装MySQL**
   - Windows：运行安装程序，选择开发者默认配置
   - Linux：使用包管理器安装
     ```bash
     sudo apt update
     sudo apt install mysql-server-8.0
     ```

3. **配置数据库**
   ```sql
   -- 登录MySQL
   mysql -u root -p
   
   -- 创建数据库
   CREATE DATABASE smartlink_index CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   
   -- 创建用户并授权
   CREATE USER 'smartlink_index'@'localhost' IDENTIFIED BY 'smartlink_index_2026';
   GRANT ALL PRIVILEGES ON smartlink_index.* TO 'smartlink_index'@'localhost';
   FLUSH PRIVILEGES;
   ```

### 2.3 Node.js环境安装

1. **下载Node.js**
   - 访问 [Node.js官网](https://nodejs.org/)
   - 下载LTS版本（16.x或更高）

2. **安装Node.js**
   - Windows/macOS：运行安装程序
   - Linux：使用包管理器
     ```bash
     curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
     sudo apt-get install -y nodejs
     ```

3. **验证安装**
   ```bash
   node -v
   npm -v
   ```

### 2.4 Maven安装（可选）

如果系统未自带Maven：

1. **下载Maven**
   - 访问 [Maven官网](https://maven.apache.org/download.cgi)
   - 下载二进制包

2. **安装Maven**
   ```bash
   # 解压
   tar -xzf apache-maven-3.9.6-bin.tar.gz
   sudo mv apache-maven-3.9.6 /opt/maven
   
   # 配置环境变量
   export M2_HOME=/opt/maven
   export PATH=$M2_HOME/bin:$PATH
   ```

3. **验证安装**
   ```bash
   mvn -version
   ```

## 三、项目安装

### 3.1 获取项目代码

```bash
# 克隆项目仓库
git clone https://github.com/your-repo/SmartLink_Index.git
cd SmartLink_Index
```

### 3.2 数据库初始化

1. **执行数据库脚本**
   ```bash
   # 进入数据库脚本目录
   cd database
   
   # 登录MySQL
   mysql -u smartlink_index -p smartlink_index_2026
   
   # 执行初始化脚本
   SOURCE init.sql;
   SOURCE sample_data.sql;  # 可选：加载示例数据
   ```

2. **验证数据库**
   ```sql
   USE smartlink_index;
   SHOW TABLES;
   ```

### 3.3 后端服务安装

1. **进入后端目录**
   ```bash
   cd zhilian-backend
   ```

2. **配置应用**
   - 复制配置文件模板
     ```bash
     cp src/main/resources/application-dev.yml.template src/main/resources/application-dev.yml
     ```
   
   - 修改数据库连接配置
     ```yaml
     # 编辑 application-dev.yml
     spring:
       datasource:
         url: jdbc:mysql://localhost:3306/smartlink_index?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
         username: smartlink_index
         password: smartlink_index_2026
     ```

3. **构建项目**
   ```bash
   # 清理并构建
   mvn clean package -DskipTests
   
   # 或者使用Maven Wrapper（如果项目有）
   ./mvnw clean package -DskipTests
   ```

4. **启动服务**
   ```bash
   # 方式一：使用Java直接运行
   java -jar target/zhilian-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev
   
   # 方式二：使用Maven插件运行
   mvn spring-boot:run -Dspring-boot.run.profiles=dev
   ```

5. **验证后端服务**
   - 访问 Swagger文档：http://localhost:8080/api/swagger-ui.html
   - 测试API：http://localhost:8080/api/v3/api-docs

### 3.4 前端应用安装

1. **进入前端目录**
   ```bash
   cd zhilian-frontend
   ```

2. **安装依赖**
   ```bash
   # 使用npm安装
   npm install
   
   # 或者使用yarn（推荐）
   yarn install
   ```

3. **配置前端环境**
   - 复制环境配置文件
     ```bash
     cp .env.development.template .env.development
     ```
   
   - 修改API接口地址
     ```env
     # 编辑 .env.development
     VITE_API_BASE_URL=http://localhost:8080/api
     ```

4. **启动开发服务器**
   ```bash
   # 启动开发服务器
   npm run dev
   
   # 或者使用yarn
   yarn dev
   ```

5. **构建生产版本**
   ```bash
   # 构建生产版本
   npm run build
   
   # 预览构建结果
   npm run preview
   ```

6. **验证前端应用**
   - 开发环境：http://localhost:5173
   - 预览环境：http://localhost:4173

## 四、服务启动与验证

### 4.1 启动顺序

1. **启动MySQL服务**
   ```bash
   # Windows
   net start mysql
   
   # Linux/macOS
   sudo systemctl start mysql
   ```

2. **启动后端服务**
   ```bash
   cd zhilian-backend
   mvn spring-boot:run -Dspring-boot.run.profiles=dev
   ```

3. **启动前端服务**
   ```bash
   cd zhilian-frontend
   npm run dev
   ```

### 4.2 服务验证

1. **数据库连接验证**
   ```bash
   mysql -u smartlink_index -p smartlink_index_2026 -e "SELECT 1;"
   ```

2. **后端服务验证**
   ```bash
   curl http://localhost:8080/api/test/health
   ```

3. **前端应用验证**
   - 打开浏览器访问：http://localhost:5173
   - 检查页面是否正常加载

4. **完整功能验证**
   - 用户注册/登录
   - 企业信息录入
   - 数字化诊断测试
   - 数据可视化展示

## 五、常见问题解决

### 5.1 端口冲突

如果端口被占用，可以修改配置：

```yaml
# 后端端口修改（application-dev.yml）
server:
  port: 8081  # 修改为其他端口

# 前端端口修改（vite.config.js）
export default defineConfig({
  server: {
    port: 5174  # 修改为其他端口
  }
})
```

### 5.2 数据库连接失败

1. **检查MySQL服务状态**
   ```bash
   sudo systemctl status mysql
   ```

2. **检查用户名密码**
   ```bash
   mysql -u smartlink_index -p
   ```

3. **检查数据库权限**
   ```sql
   SHOW GRANTS FOR 'smartlink_index'@'localhost';
   ```

### 5.3 构建失败

1. **清理Maven缓存**
   ```bash
   mvn clean
   rm -rf ~/.m2/repository
   ```

2. **更新依赖**
   ```bash
   mvn clean install -U
   ```

3. **检查网络连接**
   - 确保能够访问Maven中央仓库
   - 配置国内镜像源（如阿里云）

### 5.4 前端依赖安装失败

1. **清理npm缓存**
   ```bash
   npm cache clean --force
   ```

2. **使用淘宝镜像**
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```

3. **删除node_modules重新安装**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

## 六、生产环境部署

### 6.1 环境配置

1. **修改配置文件**
   ```bash
   # 使用生产环境配置
   cp src/main/resources/application-prod.yml.template src/main/resources/application-prod.yml
   ```

2. **配置生产环境参数**
   ```yaml
   # application-prod.yml
   spring:
     datasource:
       url: jdbc:mysql://prod-mysql:3306/smartlink_index?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
       username: ${DB_USERNAME}
       password: ${DB_PASSWORD}
   
   jwt:
     secret: ${JWT_SECRET}
   
   oss:
     access-key-id: ${OSS_ACCESS_KEY_ID}
     access-key-secret: ${OSS_ACCESS_KEY_SECRET}
   ```

### 6.2 构建生产包

```bash
# 后端生产包
cd zhilian-backend
mvn clean package -Dspring.profiles.active=prod

# 前端生产包
cd zhilian-frontend
npm run build
```

### 6.3 部署步骤

1. **部署后端服务**
   ```bash
   # 上传jar包到服务器
   scp target/zhilian-0.0.1-SNAPSHOT.jar user@server:/opt/zhilian/
   
   # 启动服务
   nohup java -jar zhilian-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod > app.log 2>&1 &
   ```

2. **部署前端应用**
   ```bash
   # 构建产物在 dist/ 目录
   scp -r dist/* user@server:/var/www/html/
   
   # 配置Nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           root /var/www/html;
           index index.html;
           try_files $uri $uri/ /index.html;
       }
       
       location /api {
           proxy_pass http://localhost:8080;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

## 七、技术支持

### 7.1 日志查看

```bash
# 后端日志
tail -f logs/zhilian-backend.log

# 系统日志
journalctl -u mysql -f
```

### 7.2 性能监控

- **数据库监控**：MySQL Workbench
- **应用监控**：Spring Boot Actuator
- **前端调试**：Chrome DevTools

### 7.3 备份恢复

```bash
# 数据库备份
mysqldump -u smartlink_index -p smartlink_index_2026 > backup.sql

# 数据库恢复
mysql -u smartlink_index -p smartlink_index_2026 < backup.sql
```

---

**注意**：
- 首次安装建议使用开发环境配置
- 生产环境请使用强密码和密钥
- 定期备份数据库和重要文件
- 保持系统和依赖库的更新